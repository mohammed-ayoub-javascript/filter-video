{
  "name": "video-blur-extension",
  "version": "1.0.0",
  "description": "Chrome extension for video detection",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
